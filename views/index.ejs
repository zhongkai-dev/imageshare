<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card shadow-lg">
      <div class="card-header bg-primary text-white text-center py-3">
        <h4>Welcome to File Notes</h4>
      </div>
      <div class="card-body p-4">
        <% if (typeof error !== 'undefined') { %>
          <div class="alert alert-danger" role="alert">
            <%= error %>
          </div>
        <% } %>
        
        <p class="mb-4 text-center">Enter your 6-digit user ID to login or register.</p>
        
        <form action="/auth/login" method="POST">
          <div class="mb-3">
            <label for="userId" class="form-label">6-Digit User ID</label>
            <input type="text" class="form-control form-control-lg" id="userId" name="userId" 
                  placeholder="Enter any 6 digits" pattern="[0-9]{6}" maxlength="6" required>
            <div class="form-text">Enter any 6-digit code of your choice (e.g., 123456, 000000, etc.)</div>
          </div>
          
          <div class="d-grid gap-2">
            <button type="submit" class="btn btn-primary btn-lg">Login / Register</button>
          </div>
        </form>
        
        <div class="mt-4 text-center">
          <p class="mb-0">First time? Simply enter any 6-digit number to create your account.</p>
          <p class="text-muted small">Remember your number to access your files in the future.</p>
          <div class="alert alert-info mt-3">
            <i class="fas fa-info-circle me-2"></i> Multiple users can use different 6-digit codes to access their own files.
          </div>
        </div>
      </div>
    </div>
  </div>
</div> 